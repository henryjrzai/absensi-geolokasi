import { Text } from "react-native-paper";
import { SafeAreaProvider } from "react-native-safe-area-context";

export default function Pengajuan() {
  return (
    <SafeAreaProvider>
      <Text>Pengajuan Absensi</Text>
    </SafeAreaProvider>
  )
}